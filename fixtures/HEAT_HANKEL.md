# HHT-001：正热迹与未加权 Hankel 正性的精确分界

## 状态与本轮问题

这是一个公开合成边界样例。下文给出自包含证明草稿及有限精确算术回归测试，
不创建 canonical Problem / Attempt / Result / Solution，不代表证明 RH。
独立数学审查、Lean 内核验证和新颖性核查尚未完成；不主张文献首创。

本轮的问题不是重复算一个矩阵，而是：一个实负零点与一对非实共轭零点组成的
三次多项式，何时具有处处正的热迹？其任意移位三阶 Hankel 行列式能否为正？

## 1. 冻结定义与量词

对任意实数 a,b,c>0，记 D=b²+c²，并定义

\[
 G(z)=(1+z/a)(1+2bz/D+z^2/D).
\]

它的零点是 -a、-b+ic、-b-ic。定义

\[
 \Theta(t)=e^{-at}+2e^{-bt}\cos(ct),\qquad t>0,
\]

以及在零点附近的解析展开

\[
 \frac{G'(z)}{G(z)}=\sum_{n\ge0}(-1)^n\mu_n z^n.
\]

这里 mu_n 是**对数导数**的系数，不是 G 自身的 Taylor 系数。
具体地，令 alpha=1/a，beta=1/(b-ic)，则

\[
 \mu_n=\alpha^{n+1}+\beta^{n+1}+\bar\beta^{n+1}.
\]

矩阵下标约定：H_{d,k}=(mu_{k+i+j})_{0<=i,j<d}，
B_{d,k}=((k+i+j)! mu_{k+i+j})_{0<=i,j<d}。
所有关于 d,k 的全称结论分别量化 d>=1、k>=0 的整数。

## 2. 结论 A：热迹正性的充要条件

令 r=(b-a)/c。存在唯一的 r_*>0，满足

\[
 \log 2-r_*(\pi-\arctan r_*)-\tfrac12\log(1+r_*^2)=0.
\]

则

\[
 \Theta(t)\ge0\ (\forall t>0)\quad\Longleftrightarrow\quad r\ge r_*,
\]

\[
 \Theta(t)>0\ (\forall t>0)\quad\Longleftrightarrow\quad r>r_*.
\]

r_* 约为 0.2289034698；这个小数仅供理解，不是认证区间，证明使用隐式精确定义。

### 证明

令 x=ct>0，除去正因子 e^{-at} 后，只需判断
1+2e^{-rx}cos x 的符号。

当 r<=0 时，取 x=pi，得到 1-2e^{-r pi}<0，故非负性不成立。

当 r>0 时，cos x>=0 的区间无需检查。第一段负余弦区间内，
f(x)=-e^{-rx}cos x 在端点为零，且

\[
 f'(x)=e^{-rx}(r\cos x+\sin x).
\]

唯一最大点是 x_*=pi-arctan r，最大值是

\[
 M(r)=\frac{e^{-r(\pi-\arctan r)}}{\sqrt{1+r^2}}.
\]

之后第 j 段负余弦区间的最大值为 e^{-2j pi r} M(r)，严格更小。
因此全域非负的充要条件是 2M(r)<=1；全域严格正的充要条件是 2M(r)<1。

令 F(r)=log(2M(r))。直接求导有
F'(r)=-(pi-arctan r)<0，F(0)=log 2>0，且 F(r)->-infinity。
故恰有一个根 r_*。当 r=r_* 时，仅在 t=(pi-arctan r_*)/c 处取零；
其余 t>0 处均为正。等号情形不可写成“处处严格正”。证毕。

## 3. 结论 B：所有移位三阶 Hankel 行列式都为负

对所有 a,b,c>0 和整数 k>=0，

\[
 \boxed{\det H_{3,k}
 =-\frac{4c^2((a-b)^2+c^2)^2}
 {a^{k+5}(b^2+c^2)^{k+5}}<0.}
\]

这条结论不要求热迹非负；它在整个定义域成立。

### 证明

令 V 的三列依次为 (1,u,u²)^T，其中 u=alpha,beta,conj(beta)。则

\[
 H_{3,k}=V\,\operatorname{diag}
 (\alpha^{k+1},\beta^{k+1},\bar\beta^{k+1})\,V^T.
\]

注意这里是普通转置 V^T，不是共轭转置；误换为共轭转置会丢掉关键负号。

Vandermonde 行列式公式给出

\[
 \det H_{3,k}=(\alpha\beta\bar\beta)^{k+1}
 (\beta-\alpha)^2(\bar\beta-\alpha)^2(\bar\beta-\beta)^2.
\]

分别计算

\[
 \alpha\beta\bar\beta=\frac1{aD},\qquad
 |\beta-\alpha|^2=\frac{(a-b)^2+c^2}{a^2D},\qquad
 (\bar\beta-\beta)^2=-\frac{4c^2}{D^2},
\]

代入即得结论。因 a,b,c>0，分子中除显式负号外的因子均严格正。证毕。

标准代数输入：NIST DLMF §1.3(ii), equation 1.3.13，Vandermonde 行列式恒等式：
https://dlmf.nist.gov/1.3.E13 （核对日期：2026-09-08）。
这里的参数代入和热迹分析是本文件给出的推导，不把该参考资料描述为本研究的背书。

## 4. 结论 C：阶乘加权的全部矩阵可以正定

当 r>=r_* 时，对全部 d>=1、k>=0 有 B_{d,k} 正定，
但全部 H_{3,k} 都有负行列式，因而不是半正定。

### 证明

由于 a,b>0，以下积分绝对收敛，逐项积分是有限项运算：

\[
 n!\mu_n=\int_0^\infty t^n\Theta(t)\,dt.
\]

任取非零实向量 v，令 p(t)=sum_{j=0}^{d-1}v_j t^j。则

\[
 v^TB_{d,k}v=\int_0^\infty t^k p(t)^2\Theta(t)\,dt>0.
\]

严格正性来自：p 是非零多项式，零点有限；Theta 连续、非负，并在某个开区间严格正。
因此即使 r=r_* 存在一个孤立零点，积分仍严格为正。
结合结论 B 即得反例族。证毕。

**被排除的推理**：不能从“所有阶乘加权 Hankel 矩阵正定”出发，
仅通过逐项除去阶乘，就宣布未加权矩阵也正定。
把移位 k 增大不能修复这条推理：本反例族对每个 k>=0 都有负的三阶行列式。
这不是对真实 xi 函数的反例，也不排除利用其额外结构的证明。

## 5. 明确实例：a=1, b=2, c=1

此时 G(z)=(1+z)(1+4z/5+z²/5)，Theta=e^{-t}+2e^{-2t}cos t。
不依赖阈值小数也能证明 Theta>0：当 0<t<=1 时 cos t>0；当 t>=1 时，
1+2e^{-t}cos t >=1-2/e>0。

前五项为 (9/5,31/25,129/125,611/625,3049/3125)，并且

\[
 \det H_{3,k}=-16/5^{k+5}\quad(\forall k\ge0).
\]

取 v=(2/5,-7/5,1)^T，有 v^T H_{3,0} v=-44/3125。
此外 Theta''''(0)=-13；连续性表明它在足够小的正 t 上也为负，
所以这个正热迹并不完全单调。

## 6. 复现、证据与边界

在仓库根目录运行：

```bash
# 标准库测试；外层有墙钟、CPU、内存及输出文件大小限制。
(ulimit -v 524288; ulimit -t 20; ulimit -f 128; \
 timeout 30s python3 scripts/test_heat_hankel.py)
```

测试共 12 项，包括：两种矩系数算法的 351 个精确等式检查；
27 个有理参数组、9 个移位的 243 个行列式检查；
固定实例的 17 个移位；30 个阶乘加权矩阵的全部顺序主子式；
负二次型、四阶导数、有限谱秩界和非法输入/资源边界。

所有数值使用 Fraction，不用浮点特征值判定正负。
测试没有认证 r_* 的小数，也没有靠有限枚举证明全称命题。
通用结论的依据是上文证明草稿；两种算法仍由同一研究过程编写，
不等于完成了独立数学审查。没有调用付费模型、网络或外部 CAS。

## 7. GitHub 迭代记录与下一任务

HHT-001 本轮状态：精确命题已冻结、证明草稿完成、有限精确检查通过；独立复核待办。
仓库 Issues 未开启时，由此文件和 PR 的任务清单承载工作项，而不是假称已创建 Issue。

- [x] 定义对数导数矩序列，避免与原多项式系数混淆。
- [x] 推导热迹非负/严格正的充要条件，处理边界等号。
- [x] 推导任意移位的行列式公式并交叉检查。
- [ ] 独立复核以上三段证明与量词；不得由测试通过自动勾选。
- [ ] 文献查重；目前不主张新颖性。

**HHT-002（下一轮，未执行）**：对具有多个非实共轭零点对的有限谱，
明确假设与重数后，研究满秩 Hankel 矩阵的正负惯性与共轭对个数的关系。
先做文献核对及有限谱命题，不能直接外推到无限零点谱。

本文件不启动后台循环。每轮应提交冻结命题、可复现计算、证明缺口和下一任务，
而不是把一次运行结束写成定理已经正式准入。
