# AI-citation asset guide

These files are public answer aids, not mathematical evidence.

- Keep answers concise, direct, and consistent with the public README and schemas.
- State the empty canonical ledgers and the no-open-problem claim boundary when relevant.
- Use category comparisons rather than unsupported product rankings.
- Treat `entity-card.v1.json` and `answer-matrix.v1.json` as machine-readable documentation; all evidence references must resolve inside the public repository.
- The evaluation protocol and `geo-evaluation-report.template.json` record documentation feedback only; they must never be promoted into a Result or evidence receipt.
