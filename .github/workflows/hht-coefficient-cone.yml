name: Xi coefficient balance certificates

on:
  pull_request:
    paths:
      - 'scripts/hht_coefficient_cone.py'
      - 'scripts/hht_balance.py'
      - 'scripts/test_hht_balance.py'
      - 'scripts/certify_xi_balance.py'
      - 'scripts/test_hht_coefficient_cone.py'
      - 'scripts/check_hht_coefficient_cone.py'
      - 'fixtures/lean-proof/HHTCoefficientCone.lean'
      - 'fixtures/HHT_COEFFICIENT_CONE.md'
      - '.github/workflows/hht-coefficient-cone.yml'

permissions:
  contents: read

concurrency:
  group: hht-coefficient-cone-${{ github.ref }}
  cancel-in-progress: true

jobs:
  coefficient-arithmetic:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: '3.12'
      - name: Install locked ball arithmetic
        run: |
          timeout 180s python3 -m pip install --only-binary=:all: 'python-flint==0.8.0'
      - name: Exact finite regressions
        run: |
          timeout 40s python3 scripts/test_hht_coefficient_cone.py
          timeout 40s python3 scripts/test_hht_balance.py
      - name: Recheck the inherited sequence input and exact coefficient condition
        run: |
          ulimit -v 2097152
          ulimit -t 300
          ulimit -f 8192
          timeout 240s python3 scripts/certify_xi_shifts.py > /tmp/hht-shift-prerequisite.txt
          tail -n 1 /tmp/hht-shift-prerequisite.txt
          timeout 40s python3 scripts/hht_coefficient_cone.py --family 25
          timeout 300s python3 scripts/certify_xi_balance.py --summary
  coefficient-kernel:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: '3.12'
      - name: Install locked Lean
        run: |
          timeout 300s curl --connect-timeout 15 --max-time 240 --proto '=https' --tlsv1.2 -sSf -o /tmp/elan-init.sh https://raw.githubusercontent.com/leanprover/elan/464c9d28395000a2a0128e07081e4956d50eced2/elan-init.sh
          echo 'a620ff1641616222c8d37c54845492004bb84d6877cdbc944dd65c1aa685bf53  /tmp/elan-init.sh' | sha256sum --check --strict
          timeout 600s sh /tmp/elan-init.sh -y --default-toolchain none
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"
          timeout 900s "$HOME/.elan/bin/elan" toolchain install leanprover/lean4:v4.33.0
      - name: Restore fixed Mathlib
        working-directory: fixtures/lean-proof
        run: timeout 900s lake exe cache get
      - name: Compile and audit all seventeen coefficient inequalities
        run: timeout 300s python3 scripts/check_hht_coefficient_cone.py
